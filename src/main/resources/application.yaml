server:
  port: 8090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/locadora-autenticacaodeusuario
    username: postgres
    password: vini4222
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.PostgreSQLDialect
      jdbc:
        lob.non-contextual-creation: true
    properties:
      hibernate:
        show_sql: true